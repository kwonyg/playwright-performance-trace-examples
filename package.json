{
  "name": "playwright-performance-trace-example",
  "license": "UNLICENSED",
  "scripts": {
    "test": "npx playwright install && npx playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.22.2",
    "typescript": "^4.7.2"
  }
}
